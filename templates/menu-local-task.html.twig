{#
/**
 * @file
 * Template for a local task link.
 */
#}
<li{{ attributes.addClass('nav-item') }}>
  <a href="{{ link['#url'] }}" class="btn btn-light mr-2 {{ is_active ? 'active' }}">{{ link['#title'] }}</a>
</li>
